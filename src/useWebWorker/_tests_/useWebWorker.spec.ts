import useWebWorker from '../index';

describe('useWebWorker', () => {
  test('import useWebWorker from "hookdafish/useWebWorker"', () => {
    expect(typeof useWebWorker).toBe('function');
  });
});
